import * as React from 'react';
import { Button } from 'react-native-paper';

export default () => (
  <Button
    onPress={() => null}
    compact
    color="#adadad"
    style={{ marginTop: 16 }}
  >
    Continuar como convidado
  </Button>
);
